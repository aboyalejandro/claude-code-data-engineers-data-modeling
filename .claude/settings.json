{
  "hooks": {
    "PreCommit": [
      {
        "command": "cd dbt && python3 -m pytest tests/ -x -q 2>/dev/null; dbt test --select state:modified 2>/dev/null || true"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "if echo $FILE_PATH | grep -q '\\.sql$'; then sqlfluff lint --dialect postgres $FILE_PATH 2>/dev/null || true; fi"
      }
    ]
  }
}
